# 原生 Trime2 实时预览键盘主题编辑器功能汇总

> 本文是原生 Android Trime2 主题编辑器的功能规格汇总。
>
> 目标是让用户直接创建、编辑、实时预览、保存和导出 Trime2 Lua 主题,而不是只做一个颜色配置工具。
>
> 参数依据:[主题Lua参数文档.md](主题Lua参数文档.md),对应仓库当前 `develop` 分支提交 `97f0a8d5`。

## 1. 产品目标

编辑器应支持完整工作流:新建主题、导入主题、可视化编辑键盘和按键、编辑样式和资源、实时预览、语法检查、兼容性检查、保存和导出。

核心目标:

- 在手机上增加、删除、复制、移动和调整按键。
- 编辑按键文字、图标、事件、长按、滑动、尺寸和样式。
- 设置键盘背景图、按键背景图、标签图标、字体、音效和振动。
- 预览中文、英文、候选栏、翻页、长按、符号面板、剪贴板和组合窗。
- 对当前 Trime2 能解析但不能触发的参数显示警告。

## 2. 项目结构和页面

### 2.1 主题目录

```text
主题目录/
├── main.lua
├── styles/<style>/main.lua
├── keyboards/<keyboard>.lua
├── fonts/
├── images/
├── sounds/
└── scripts/
```

### 2.2 页面

- 主题设置:名称、作者、默认样式、默认键盘、动态键盘规则。
- 键盘编辑:布局、行、容器和按键增删改。
- 按键属性:显示、布局、事件、长按、滑动和反馈。
- 样式编辑:按键、候选栏、工具栏、符号、剪贴板和组合窗。
- 资源管理:图片、字体、音效和脚本。
- 实时预览:交互预览和状态模拟。
- Lua 代码:查看或高级编辑生成的 Lua。
- 验证导出:语法、引用、资源和兼容性检查。

主界面建议使用原生 Android `Activity`、`Fragment`、`ViewModel`、`StateFlow` 和自定义 View,不使用 WebView 作为主要编辑器。

## 3. 主题项目管理

支持:

- 新建主题并复制内置默认主题作为模板。
- 打开主题目录、ZIP 包或单个 `main.lua`。
- 自动寻找相邻的 `styles`、`keyboards` 和资源目录。
- 最近项目列表。
- 自动保存未完成编辑状态。
- 保存前生成恢复快照。
- 显示未保存修改标记。
- 不直接修改内置默认主题。
- 导出到独立主题目录,避免覆盖已有主题。

新建向导可设置主题名称、作者、默认样式、默认键盘,以及是否生成浅色、深色、英文、数字和符号模板。

## 4. 键盘管理

### 4.1 键盘列表

用户可以:

- 增加、删除、复制和重命名键盘。
- 编辑键盘名称、作者和样式引用。
- 设置主题默认键盘。
- 为不同方案配置不同键盘。
- 在键盘之间复制整行、容器或按键。
- 从模板导入键盘。

删除前检查是否被默认键盘、`get_keyboard()` 或其他配置引用。

### 4.2 布局模式

Trime2 按 `rows > flex_box > keys > key_maps` 选择布局。编辑器必须显示当前布局模式,不能把多个模式同时当作有效布局。

| 模式 | 编辑功能 | 适用场景 |
|---|---|---|
| `rows` | 行和按键增删、排序、百分比调整 | 普通键盘、九宫格、数字键盘 |
| `flex_box` | 容器嵌套、方向、尺寸和权重 | 复杂自适应布局 |
| `keys` | 自由拖动、定位和缩放 | 绝对布局 |
| `key_maps` | 分页、页名和符号按键 | 符号面板 |

切换布局模式时提示将删除、迁移或隐藏其他布局数据。

### 4.3 `rows` 行式布局

支持:

- 增加、删除、复制、上移和下移行。
- 拖动调整行顺序。
- 设置行高度百分比。
- 设置行默认键宽百分比。
- 行内增加、删除、复制、粘贴和排序按键。
- 跨行移动按键。
- 设置单键宽度和高度。
- 自动平均分配行宽和行高。
- 显示行宽和高度检查。

```lua
rows = {
  {
    height = 25,
    width = 10,
    keys = {
      { click = 'q' },
      { click = 'w' },
      { click = 'e' },
    },
  },
}
```

### 4.4 `flex_box` 弹性布局

支持:

- 增加横向或纵向容器。
- 删除、复制和移动容器。
- 将容器拖入其他容器。
- 设置 `direction`、`width`、`height`、`grow` 和 `style`。
- 容器内增加、删除、复制和排序按键。
- 显示递归层级和固定尺寸冲突。

兼容性提示:只有精确值 `column` 是纵向布局;其他值按 `row` 处理。固定主轴尺寸大于 `0` 时,Trime2 会把对应方向的 `grow` 强制设为 `0`。

### 4.5 `keys` 绝对布局

支持:

- 画布增加、删除、复制和拖动按键。
- 调整按键边界。
- 设置 `x`、`y`、`width`、`height` 百分比。
- 显示网格、参考线和吸附线。
- 左右上下和中心对齐。
- 等间距分布。
- 多选后批量移动、缩放和设置样式。
- 锁定按键位置。

### 4.6 `key_maps` 符号分页

支持:

- 增加、删除、复制和排序符号页。
- 修改页名称。
- 页内增加、删除、复制和排序符号按键。
- 字符选择器批量添加符号。
- 预览 Tab 标题、工具栏和分页按钮。

## 5. 按键编辑

### 5.1 基础属性

每个按键支持:

- 主文字 `label`。
- 助记 `hint`。
- 无障碍说明 `description`。
- 样式引用 `style`。
- 宽度、高度、`x`、`y`。
- 位置锁定。
- 文字层显示开关。

支持显示文字和事件名称分离:

```lua
{
  click = 'BackSpace',
  label = '删除',
  description = '删除前一个字符',
  style = 'functional',
}
```

### 5.2 增加、删除、复制

支持:

- 增加空白按键。
- 从预设事件列表增加按键。
- 快速插入空格、回车、删除、Shift、数字、符号和方向键。
- 删除当前按键或批量删除。
- 复制、粘贴和跨行复制。
- 复制到其他键盘。
- 拖放改变顺序。
- 撤销删除。

删除必须真正从布局数组移除节点,不能只隐藏 UI。

### 5.3 有效事件

当前 Trime2 实际有效的按键事件为:

- `click`
- `long_click`
- `swipe_left`
- `swipe_right`
- `swipe_up`
- `swipe_down`
- `combo`

事件编辑器支持发送按键、上屏文本、提交文本、执行命令、切换 Rime 选项、选择键盘或页面、调用 Lua 脚本、引用 `preset_keys` 和内联事件表。

事件字段控件:

| 字段 | 控件 | 用途 |
|---|---|---|
| `send` | 按键选择器和组合键录入 | 发送 Android/Rime 按键 |
| `text` | 多行文本框 | 直接上屏 |
| `commit` | 多行文本框 | 提交文本 |
| `command` | 命令输入框 | 执行命令或脚本 |
| `option` | 文本框 | 命令参数 |
| `select` | 选择器 | 选择目标功能 |
| `toggle` | 文本框 | 切换 Rime 开关 |
| `states` | 字符串列表 | 状态显示文本 |
| `label` | 文本框 | 事件显示文字 |
| `preview` | 文本框 | 按下预览文字 |
| `description` | 文本框 | 功能和无障碍说明 |
| `shift_lock` | 枚举选择 | Shift 锁定方式 |
| `sticky` | 开关 | 保持锁定 |
| `repeatable` | 开关 | 按住重复 |
| `functional` | 开关 | 功能键语义 |

`double_click` 和 `triple_click` 不应出现在有效事件列表,当前版本不解析它们。

### 5.4 状态替代

支持编辑:

- `ascii`:ASCII 模式替代。
- `composing`:组字时替代。
- `has_menu`:有候选菜单时替代。
- `paging`:翻页时替代。
- `send_bindings`:是否使用 Rime 按键绑定。

预览区提供中文、ASCII、组字、有候选和翻页状态开关。

### 5.5 长按

支持:

- 长按事件和 `hint_long`。
- 长按触发时间。
- 弹出字符串或弹出按键列表。
- 弹出列数、键宽、键高。
- 弹出面板背景、圆角、边框和阴影。
- 实时触发长按预览。

```lua
{
  click = 'a',
  popup = 'áàǎâāä',
  long_click = 'VOICE_ASSIST',
  hint_long = '语音',
}
```

字符串形式会逐字符拆分为弹出项;数组形式按给定顺序显示。当前 `isLongPressPopup()` 固定返回 `false`,数组不会依赖该路径自动追加大小写变体。

### 5.6 滑动

支持左、右、上、下事件、四方向提示、重复触发和交互预览。

`swipe` 滑动点按映射当前因 `isKeySwipeTap()` 固定返回 `false` 而不可达,编辑器必须标记警告。

### 5.7 组合事件

可编辑和导出 `combo`,但当前 Java 虽会解析和保存,没有确认到 KeyView 触发路径。编辑器应显示“可保存,但当前版本实时触发可能无效”。

## 6. 样式编辑

### 6.1 通用按键样式

支持编辑:

- 文字颜色、文字大小和字体。
- 背景颜色或背景图片。
- 圆角、边框宽度、边框颜色。
- 阴影高度和阴影颜色。
- 内容对齐、X/Y 偏移。
- 外边距和内边距。
- `scale_x`、`scale_y`。
- `translation_x`、`translation_y`、`translation_z`。
- `show`、振动和音效。

### 6.2 普通态和按下态

使用双栏或 Tab 编辑普通态与 `pressed` 态:

- 背景和文字颜色。
- 圆角、边框和阴影。
- 缩放、位移和 Z 轴效果。
- 助记、长按标签和方向提示的按下态。

预览点击按键时必须立即切换到 `pressed` 样式。

### 6.3 提示层

支持独立编辑:

- `key.hint`。
- `key.hint.up`、`down`、`left`、`right`。
- `key.long_click`。
- `key.preview`。

每个提示层支持颜色、大小、字体、位置、偏移和显示开关。

### 6.4 自定义样式集合

支持新建、重命名、复制、删除和批量替换样式,显示样式继承、引用数量和未引用样式。

```lua
normal = { background = 0xffffffff, text_color = 0xff222222 }
functional = { background = 0xff444444, text_color = 0xffffffff }
danger = { background = 0xffb00020, text_color = 0xffffffff }
```

删除被引用样式前必须选择替换样式或取消操作。

## 7. 图片和资源

### 7.1 键盘和组件背景图

可为以下对象设置颜色或图片:

- 主题根背景。
- 键盘背景。
- 单个按键和按下态。
- 候选栏和候选按下态。
- 工具栏。
- 符号面板。
- 剪贴板面板。
- 组合窗。
- 长按弹出面板。

资源管理支持导入 PNG、WebP、JPG 等 Android 支持的图片、系统相册导入、复制到主题 `images/`、重命名、删除、预览、引用检查和缺失警告。

`background` 导出为颜色整数或相对图片文件名,不把二进制图片写入 Lua。图片查找先看当前样式目录,再看主题 `images/`;按键标签图片还会查共享数据目录。

### 7.2 按键图片和图标

支持两种方式:

1. 将图片设置为按键 `background`。
2. 将图片命名为与 `label` 相同的 PNG,由 Trime2 自动把标签显示成图标。

```lua
space = {
  label = 'space_icon',
  text_color = 0xff000000,
}
```

当 `images/space_icon.png` 存在时,可显示为图标并按 `text_color` 染色。编辑器支持选择图标、设置图标标签、图标颜色、图标尺寸(由 `text_size` 控制)、按下态颜色和缺失提示。

资源列表应显示文件名、类型、尺寸、大小、引用者、冲突和可否安全删除。

### 7.3 字体

支持导入 TTF/OTF、设置键盘/按键/候选/组合窗字体、设置 fallback 数组、预览中英文和符号覆盖情况、检查字体缺失。字体导出到 `fonts/`;Android 10/API 29 以下显示 fallback 兼容性提示。

### 7.4 音效和振动

支持导入和试听音效、启用按键音、指定普通态和长按音效、检查引用。`sound_effect` 先查当前样式目录,再查主题 `sounds/`。

支持启用振动、编辑 `timings` 和 `amplitudes`、校验长度、将振幅限制为 `0..255` 和设备试听:

```lua
vibration_enabled = true
vibration_effect = {
  timings = { 0, 20 },
  amplitudes = { 0, 120 },
}
```

## 8. 候选栏、工具栏和面板

### 8.1 候选栏

支持编辑高度、背景、文字颜色、文字大小、字体、阴影、候选按下态、注释、展开按钮文字/图标/样式、展开面板、筛选栏、工具栏和工具栏按键。

预览状态:无候选、单行、多行、带注释、按下、展开、筛选、翻页。

### 8.2 工具栏

支持编辑背景、文字、高度、阴影、隐藏按钮、普通按钮样式、顺序、增删复制按钮、预设键、直接事件表、Rime 方案开关及其名称、选项、状态和重置值。

`candidate.expanded.tool_bar.keys`、`symbol.tool_bar.keys`、`clipboard.tool_bar.keys` 实际是字符串数组,编辑器不应提供不兼容的直接事件表入口。

### 8.3 符号面板

支持符号背景、文本、按键、Tab 位置/高度/指示器、工具栏、页面名称和排序、符号按键增删复制排序、字符选择器批量添加。

### 8.4 剪贴板面板

支持背景、内容项文字和内边距、Tab 栏、指示器、工具栏、工具栏按键以及撤销按钮预览。

### 8.5 预编辑和组合窗

预编辑支持颜色、大小、背景、字体、独立显示、嵌入输入框、嵌入组合区和预览模式。

组合窗支持显示、位置、固定/拖动/一次拖动、最小触发长度、最大词长、固定行数、候选数、云候选、全部词语、当前候选、宽高限制、内边距、行间距、文字样式、按下态、编号格式、候选格式、注释格式、分隔符、对齐和点击事件。

实时预览提供普通组合、候选列表、翻页、有候选、拖动位置和不同长度文本状态。

## 9. 主题级功能

### 9.1 `action_labels`

提供表单编辑默认 Enter、发送、前往、完成、搜索、上一个和下一个文字,并切换不同 Editor Action 检查动态标签。

### 9.2 `get_keyboard(id, alphabet)`

提供规则向导和高级 Lua 编辑:

- 按方案 ID 选择键盘。
- 按 alphabet 选择键盘。
- 数字输入切换数字键盘。
- 英文输入切换英文键盘。
- 特定方案切换专用键盘。

### 9.3 生命周期和语音回调

可查看和编辑 `onWindowShown`、`onWindowHidden`、`onStartInput`、`onFinishInput`、`onConfigurationChanged`、`onDestroy` 和 `onSpeechResults`。编辑器必须提示这些回调运行在输入法服务生命周期,不应执行耗时操作或泄露输入内容。

## 10. 实时预览系统

### 10.1 实现原则

优先复用 Trime2 原生 LuaJ 主题加载和 Java View 逻辑,不要在编辑器中另写一套近似渲染器。

推荐流程:

1. 编辑器维护内存中的结构化主题模型。
2. 将模型导出到临时主题目录。
3. 使用与 Trime2 相同的 LuaJ 和主题解析路径。
4. 创建原生 `InputView`、候选栏、工具栏和面板 View。
5. 属性变化后增量刷新;不能增量刷新时重建预览 View。

### 10.2 预览模式

提供中文、英文、ASCII 锁定、数字、有候选、翻页、组字、长按、滑动、符号、剪贴板、展开候选、工具栏、组合窗、横屏、竖屏和夜间模式。

### 10.3 交互预览

预览按键必须支持点击、长按、四方向滑动、候选点击、状态切换、打开符号/剪贴板、展开候选、按下态和预览气泡。

`combo`、`swipe` 等不可达功能显示灰色禁用状态及原因,不能伪造成功效果。

### 10.4 差异提示

提示 Android API、字体 fallback、系统字体缩放、屏幕密度、振动引擎、图片缺失和静音模式带来的设备差异。

## 11. Lua 生成和代码编辑

内部使用结构化模型保存主题、样式、键盘、行、容器、按键、事件和资源引用,不要通过脆弱的字符串替换修改 Lua。

高级代码页支持:

- Lua 语法高亮、行号、搜索替换。
- 错误行定位和格式化。
- 代码到模型、模型到代码的同步。
- 无法解析的自定义 Lua 原文保留并显示警告。

生成规则:

- 稳定字段顺序。
- 正确转义字符串、换行和反斜杠。
- Lua 保留字使用方括号,例如 `['end']`。
- 颜色输出为十六进制 ARGB。
- 区分空表、空数组和 `nil`。
- 资源使用主题相对路径。
- 删除按键时删除真实布局节点。

## 12. 撤销、重做和批量编辑

所有布局、属性、事件、图片、字体、音效、样式和资源删除操作都支持撤销重做。连续拖动和颜色拖动合并为一个撤销步骤。

支持多选按键后批量设置样式、尺寸、背景、文字颜色、事件、复制和跨行移动。

## 13. 验证和错误提示

### 13.1 语法和结构

检查 Lua 语法、括号和引号、入口文件、默认样式、默认键盘、布局冲突、空布局、行高、键宽、容器循环、缺少 `click`、样式引用和预设键引用。

### 13.2 资源

检查背景图片、标签图片、字体、音效、文件名、未使用资源和删除前引用关系。

### 13.3 当前版本兼容性警告

必须显示黄色或红色警告:

- `double_click`、`triple_click`:当前版本不解析。
- `combo`:可解析但没有确认触发路径。
- `swipe`:`isKeySwipeTap()` 固定返回 `false`。
- `layout`:当前没有实际布局效果。
- `symbol.flex_basis`:读取代码已注释。
- `composition.border`、`spacing`、`round_corner`、`elevation`:没有可靠消费证据。
- `candidate.expanded.pressed.ripple_color`:没有可靠消费证据。

## 14. 导入、导出和安装

### 14.1 导入

支持主题目录、ZIP、单个 Lua、图片、字体和音效。导入前显示主题信息、文件数量、资源数量、缺失引用、语法错误、不支持字段和覆盖风险。

### 14.2 导出

支持完整主题目录、ZIP、仅 Lua、仅资源、当前键盘、当前样式和预览截图。导出选项包括是否包含字体、图片、音效、脚本、未使用资源、注释和兼容性报告。

### 14.3 安装

如有权限可复制到 Trime2 主题目录、覆盖前备份、请求刷新并验证读取;否则使用 Android Storage Access Framework,不能绕过存储权限。

## 15. 推荐开发阶段

### 第一阶段:最小可用版本

1. 新建、打开、保存和导出主题。
2. `rows` 布局编辑。
3. 增加、删除、复制、移动按键。
4. 修改按键文字、事件、长按和滑动。
5. 修改普通态/按下态颜色、圆角、边框和阴影。
6. 设置键盘背景图、按键背景图和标签图标。
7. 样式复用和引用检查。
8. 中文、英文、候选栏和长按实时预览。
9. Lua 语法检查和兼容性警告。

### 第二阶段

- `flex_box`、绝对布局和符号分页。
- 候选展开、工具栏、剪贴板和符号面板。
- 图片裁剪、字体 fallback、音效和振动预览。
- 撤销重做、批量编辑和代码双向同步。

### 第三阶段

- 动态键盘选择。
- 生命周期和语音回调高级编辑。
- 主题安装、备份、分享和多主题管理。

## 16. 关键验收标准

- 可以从零创建可加载的 Trime2 主题。
- 删除按键后生成的 Lua 中该按键确实消失。
- 增加按键后可设置文字、事件和样式。
- 拖动改变顺序后保存结果保持一致。
- 可以设置键盘背景图、按键背景图和标签图标。
- 可以看到普通态、按下态、长按弹出、滑动提示和候选栏。
- 预览使用与 Trime2 相同的 Lua 字段语义。
- 语法错误和缺失资源不会导致崩溃。
- 导出的主题目录可以被 Trime2 读取。
- 不可达字段显示警告,不伪装成已生效功能。

## 17. 关键实现原则

- 界面必须美观，不用非原生，不能是套壳html
- 预览优先复用 Trime2 的 LuaJ、ThemeManager 和原生 View。
- 结构化模型优先于文本替换。
- 文件操作使用 Storage Access Framework。
- 导入、导出、保存和安装支持失败恢复。
- 不自动执行未知主题脚本,高级 Lua 回调需明确授权。
- 用户主题、输入内容、资源文件和脚本按敏感数据处理。
