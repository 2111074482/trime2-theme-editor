# Trime2主题编辑器人工测试清单

## 设备与方向

### 手机竖屏键盘布局验收

### 手机横屏键盘布局验收




### 大字体和系统字体缩放验收

### 自定义设备尺寸预览验收

## 预览状态覆盖

### 英文和 ASCII 锁定状态验收

### 数字输入和方案切换状态验收

### 无候选、单候选和多候选验收

### 候选注释和候选按下验收

### 候选工具栏和筛选栏验收

### 普通组合和不同长度组合验收

### 分页组合和拖动组合窗验收

### Editor Action 动态标签验收

### schema_name 方案名称验收

### 预览模式不修改主题结构验收

### 预览状态重置验收

## 视觉与交互

### 画布不遮挡顶部工具栏验收

### 属性面板内容不截断验收

### 横屏键盘高度合理性验收


### 资源缩略图可读性验收

### 普通态与选中态视觉区分验收

### 按下态视觉反馈验收

### 警告态与错误态视觉区分验收

### 画布缩放与平移体验验收

### 网格、标尺和辅助线体验验收

### 设备预设切换体验验收

### 按键增加、删除、复制和移动体验验收

### 多选和批量编辑体验验收


### 图层显示隐藏与定位体验验收

### 属性抽屉打开、关闭和折叠体验验收

## 键盘交互

### 点击按键预览验收

### 长按弹出面板验收

### 左滑按键事件验收

### 右滑按键事件验收

### 上滑按键事件验收

### 下滑按键事件验收

### 候选栏展开验收

### 候选项点击验收

### 翻页状态验收

### ASCII 状态验收

### 组字状态验收

### 候选菜单状态验收

### 符号面板交互验收

### 剪贴板面板交互验收

### 工具栏按钮交互验收

### 悬浮组合窗交互验收

### 组合窗拖动位置验收

## 资源与硬件

### 键盘背景图片实际显示验收

### 按键背景图片实际显示验收

### 按键标签同名 PNG 图标验收

### 按下态背景图片验收

### 中文字体显示验收

### 英文字体显示验收

### 字体 fallback 验收

### 图片缩放和裁剪验收

### 音效导入和引用检查验收




## 系统与文件权限

### Android 文件选择器打开主题验收

### Android 文件选择器导出主题验收

### SAF 持久化 URI 权限验收

### 用户拒绝文件权限验收

### URI 权限失效后重新授权验收

### 磁盘空间不足保存失败恢复验收

### 导入损坏 ZIP 人工处理验收

### 导出覆盖已有主题前备份验收

### 安装到 Trime2 主题目录验收

### Trime2 主题刷新验收

## 真实 Trime2 对照

### 导出主题在目标 Trime2 版本导入验收

### 主题名称和作者显示对照验收

### 默认样式加载对照验收

### 默认键盘加载对照验收

### `rows` 布局显示对照验收

### `flex_box` 布局显示对照验收

### `keys` 绝对布局显示对照验收

### `key_maps` 符号分页显示对照验收

### 普通按键样式对照验收

### 按下态样式对照验收

### 候选栏样式对照验收

### 工具栏样式对照验收

### 符号面板样式对照验收

### 剪贴板面板样式对照验收

### 预编辑区样式对照验收

### 悬浮组合窗样式对照验收

### 长按弹出面板对照验收

### 图片资源路径对照验收

### 字体资源路径对照验收

### 音效资源路径对照验收

### `action_labels` 动态标签对照验收

### `get_keyboard` 动态键盘选择对照验收

### `onSpeechResults` 行为对照验收

## 项目和主题目录管理

### 新建主题向导验收

### 默认主题模板复制验收

### 浅色、深色初始模板验收

### 打开主题目录自动发现子目录验收

### 打开单个 main.lua 自动推断目录验收

### 键盘 Lua 直接返回 Android View userdata 只读预览验收

### 自定义 View userdata 导出不丢失验收

### 打开 ZIP 单层嵌套目录验收

### 最近项目列表验收

### 多主题切换验收

### 项目复制、重命名和删除验收

### 内置默认主题只读保护验收

### 未保存修改关闭拦截验收

### 自动草稿和恢复快照验收

## 键盘资产和布局迁移

### 键盘新增、删除、复制和重命名验收

### 键盘名称、作者和样式引用验收

### 键盘 lock 和 ascii_mode 验收

### 方案到键盘映射验收

### 从模板导入完整键盘验收

### 跨键盘复制整行验收

### 跨键盘复制容器验收

### 跨键盘复制按键和符号页验收

### 删除被引用键盘拦截验收

### rows 到 flex_box 迁移验收

### flex_box 到 keys 迁移验收

### keys 到 rows 迁移验收

### key_maps 模式切换保护验收

### 布局模式冲突提示验收

## 颜色和编辑操作

### ARGB 十六进制颜色输入验收

### 可视化颜色选择器验收

### 最近颜色验收

### 透明度独立编辑验收


### dp、sp、百分比和整数控件验收

### Lua integer 小数输入处理验收

### gravity 组合选择验收

### 空值、负值和 nil 语义验收

### 批量属性修改影响数量提示验收

### 粘贴按键、行和容器位置验收

### 删除引用对象影响列表验收

## 脚本和高级代码

### scripts 目录文件列表验收

### 脚本导入、重命名和删除验收

### 脚本执行风险确认验收

### 预览脚本权限隔离验收

### 脚本异常和超时处理验收

### 脚本导出风险清单验收

### 代码页跳转 RawLuaNode 验收

## 导入、导出、分享和报告

### 导入文件清单和冲突预览验收

### 同名文件跳过、覆盖和保留两份验收

### 导出文件清单和资源体积验收

### 导出到新目录避免覆盖验收

### 覆盖前备份和回滚验收

### ZIP 路径穿越和非法文件名拦截验收

### 兼容性报告导出验收


### Android 分享面板分享 ZIP 验收



### 导出完成后重新导入入口验收

### 安装前目标目录和覆盖文件提示验收

### 安装完成后入口和资源验证验收

## 主题入口和高级逻辑

### 主题名称、作者、默认样式和默认键盘编辑验收

### `action_labels` 七种回车标签验收

### `preset_keys` 新建、引用和删除验收

### `get_keyboard` 方案 ID 规则验收

### `get_keyboard` alphabet 规则验收

### 生命周期回调代码保留验收

### `onSpeechResults` 返回值说明验收

## 样式根和通用字段

### 样式根背景和总高度验收

### 键盘高度、背景和字体验收

### 样式继承和缺失回退验收

### 自定义样式新建、重命名和引用替换验收

### margins 与 padding 差异验收

### offset_x 与 offset_y 验收

### scale_x 与 scale_y 验收

### translation_x、translation_y、translation_z 验收

### long_click_time 与 repeat_click_time 验收

### vibration_effect 数组长度和振幅验收

### sound_enabled 与 sound_effect 验收

## 按键状态和弹出面板

### hint 主样式验收

### hint 上下左右样式验收

### 长按提示样式验收

### 按键预览气泡样式验收

### popup 字符串逐字符拆分验收

### popup 按键数组顺序验收

### popup 大小写变体验收

### popup 列数宽高验收

### label action_labels 动态标签验收

### label schema_name 方案名称验收

### `{KeyName}` 组合键事件验收

### `.lua` 脚本事件保留验收

## 工具栏和面板细节

### candidate comment 和 pressed 样式验收

### candidate expanded 基础样式验收

### filter_bar 显示和位置验收

### expanded tool_bar 字符串数组验收

### toolbar.hide 字符串按钮验收

### toolbar.hide 样式表按钮验收

### toolbar.schema_switches 方案开关验收

### 方案开关 name、options、states、reset 验收

### symbol.text 和 symbol.key 样式验收

### symbol Tab 指示器回退链验收

### symbol.tool_bar 默认按钮验收

### clipboard.item 内边距验收

### clipboard Tab 文字颜色回退链验收

### clipboard.tool_bar 撤销按钮验收

### preedit.inline 五种模式验收

### composition max_entries 和 cloud_max_entries 验收

### composition all_phrases 和 use_cursor 验收

### composition line_spacing 和倍率验收

### composition.window when 条件验收

### composition.window 文本格式字段验收

### composition.window align 对齐字段验收

### composition.window click 事件验收

## 第三轮边界测试

### 字符串事件与直接事件表来源保留验收

### preset_keys 引用与内联事件切换验收

### command 事件沙箱确认验收

### repeatable 与 swipe_repeatable 独立验收

### sticky 与 send_bindings 独立验收

### 显式值、继承值、默认值和回退值显示验收

### 删除显式值恢复继承验收

### 样式回退链详情验收

### 组件缺失与资源缺失分级诊断验收

### 解析任务进度和取消验收

### 解析取消后当前模型保持验收

### 并发保存任务排队验收

### 预览刷新失败保留上次有效状态验收

### 跨项目复制资源路径隔离验收

### 导入冲突处理日志和撤销验收

### ZIP 文件数量和总大小限制验收

### 预览输入上下文信息验收

### 候选、符号、剪贴板和组合窗独立状态验收

### 自定义 View userdata 高级路径验收


### 诊断报告版本和 Lua 路径验收

### 分享内容选择和隐私隔离验收

### 无障碍节点名称和值状态验收

## 第四轮实现闭环测试

### 解析接口成功、失败、取消和部分结果验收

### 写出接口成功、失败和权限拒绝验收

### 诊断接口稳定 code 验收

### ThemeDefaults 默认值来源验收

### popup 默认值集中读取验收

### candidate 默认值集中读取验收

### symbol 和 clipboard 默认工具栏验收

### composition 默认值集中读取验收

### 样式字段回退链实际来源验收

### 图片字体音效查找范围验收

### PreviewContext 状态隔离验收

### 编辑操作生成 EditCommand 验收

### 预览状态切换不生成编辑命令验收

### 后台任务取消不覆盖有效模型验收

### 导出后重新解析验证验收

### 诊断报告稳定 code 验收

### 日志敏感数据脱敏验收

### UI 字符串资源化验收

## 第五轮源码保真测试

### main.lua 先于样式 Lua 加载验收

### 主题入口全局变量被样式读取验收

### 显式键盘选择优先级验收

### get_keyboard 动态结果来源显示验收

### 样式文件失败回退 light 验收

### 多布局同时存在时实际布局选择验收

### 普通 Lua table 返回路径验收

### Android View userdata 返回路径验收

### 未知字段原文保留验收

### 未知回调原文保留验收

### 动态表达式原文保留验收

### 预设事件来源形式保留验收

### 资源查找命中路径显示验收

### 导出文件回溯模型节点验收

### 导出后字段类型和空值差异验收

### 导出后未知节点数量差异验收

## 第六轮精确语义测试

### gravity 九种值和组合值验收

### shift_lock click、double、long 枚举验收

### shift_lock double 与 double_click 区分验收

### flex_box 非 column 值按 row 处理验收

### preedit.inline 别名来源保留验收

### composition.position 十种位置验收

### composition.position 未知值回退 fixed 验收

### composition.movable 字符串类型验收

### composition.window align 别名验收

### composition.window 非法 when 原文保留验收

### Trime2 尺寸 SP 语义提示验收

### Lua integer 浮点输入阻止验收

### rows 默认宽度三级回退验收

### rows 默认高度三级回退验收

### 单键宽高继承行宽高验收

### flex_box 固定尺寸与 grow 归零验收

### composition 行距倍率零值规范化验收

### 事件空字符串与字段缺失区分验收

### sticky、repeatable、functional 默认值验收

### command 无 send 自动 function 语义验收

### index 直接事件表限定和无消费警告验收

### 按键样式 click 名称优先回退验收

### hint_long 缺省使用事件标签验收

### 四方向 hint 缺省使用事件标签验收

### send_bindings 条件事件缺省行为验收

### Popup 字符串大小写插入位置验收

### Popup 数组不追加大小写变体验收

### toolbar.keys 四种项目类型验收

### expanded tool_bar 禁止直接事件表验收

### symbol tool_bar 禁止直接事件表验收

### clipboard tool_bar 禁止直接事件表验收

### 展开候选四种内置键验收

### 符号工具栏 BackSpace 默认键验收

### 剪贴板工具栏 undo 默认键验收

### toolbar.hide 字符串和样式表来源验收

### 方案开关独立 style 兼容性提示验收

### background 颜色和图片联合类型验收

### 标签同名 PNG 染色和尺寸验收

### action_labels 非回车事件回退验收

### schema_name 非中文状态回退验收

### KeyName 组合键和 Lua 脚本识别验收

## 参数注册表人工抽查

### ThemeFieldRegistry 字段总数报告验收

### 参数文档字段零缺失报告验收

### 可视化字段属性控件映射验收

### 代码专用字段入口映射验收

### 不可靠字段兼容性 code 映射验收

### NOT_PARSED 字段隐藏无效入口验收

### PRESERVE_RAW 字段原文保留验收

### 注册表默认值与 ThemeDefaults 一致性验收

### 注册表回退链与 FallbackTrace 一致性验收

### 源码版本变化后字段审计提示验收

### 发布构建字段缺失阻断验收

## 第七轮完整工作流测试

### 项目首页最近项目和草稿入口验收

### 新建向导步骤和返回状态验收

### 主题设置到主工作台导航验收

### 样式管理页面完整操作验收

### 资源管理筛选排序和引用定位验收

### Lua 文件树和诊断跳转验收

### 验证导出页面完整流程验收

### 设置页默认目录和自动保存验收

### 页面切换保持选中节点和画布状态验收

### 进程重建恢复当前工作区验收

### 失效节点 ID 安全回退验收

### PNG WebP JPG 实际格式识别验收







### 标签图标默认 PNG 和文件名一致验收



### 点击多选全选反选和按行选择验收


### 多选统一值和混合值显示验收

### 批量修改原子撤销验收

### 内部剪贴板六种对象验收


### 系统剪贴板不泄露 URI 验收

### 磁盘指纹模型缓冲区版本验收

### 代码解析候选模型不立即覆盖验收

### 代码同步差异预览验收

### 代码与可视化冲突处理验收

### 外部文件变化冲突验收

### RawLuaNode 默认不格式化验收



### 安装目标 SAF 授权验收

### 安装前空间权限和资源检查验收

### 覆盖安装备份清单验收

### 安装后目标文件校验验收

### 无公开刷新接口时手动提示验收

### 安装失败重试继续和回滚验收


### 分享 content URI 临时权限验收

### 分享 MIME 类型一致验收


### 分享后临时文件和授权清理验收


### debug 与 release 配置隔离验收

### 版本号和 Trime2 源码版本记录验收

### 依赖许可变更日志和已知限制验收

### 正式签名密钥不入库验收

### release APK 安装和冷启动验收

### release 主工作流冒烟测试验收

### 示例主题不包含私有内容验收

### 本地崩溃日志脱敏和上传授权验收

## 第八轮构建与工程集成测试

### Gradle 9.2.0 和 AGP 8.11.0 基线构建验收

### Kotlin 2.2.0 和 JVM 11 编译验收

### minSdk 21 安装启动验收

### targetSdk 35 行为验收

### 现有 app 和 codegen 模块结构保持验收

### ThemeEditorActivity 非导出配置验收

### PrefLauncher 主题编辑器入口验收

### 输入法 Service 行为不回归验收

### 无 API_KEY 和 API_ID 开发构建验收

### debug 默认签名构建验收

### release 缺失签名明确报错验收

### 签名和 API 密钥不入库验收

### Kotest 测试发现和执行验收

### 测试依赖不进入 release APK 验收

### AndroidX instrumentation 测试运行验收

### app prebuilt JNI 日常构建验收

### BUILD_ABI 单 ABI 构建验收

### 四 ABI release 构建验收

### 未初始化 submodule 构建前诊断验收

### assets checksums 任务保持验收

### OpenCC 数据生成任务保持验收

### 原生缓存 hash 任务保持验收

### 编辑器不依赖 MANAGE_EXTERNAL_STORAGE 验收

### 现有存储权限拒绝时编辑器 SAF 可用验收

### 受控分享缓存目录验收

### FileProvider 不暴露非选择文件验收

### 分享 ClipData 和临时读取权限验收

### 现有 ToolActivity 分享功能兼容验收

### ThemeRuntimeLock 串行隔离验收

### 编辑器预览不修改当前输入法主题验收

### 编辑器预览不修改真实 Rime 状态验收

### Java null 和 LuaNil Kotlin 边界验收

### 主线程 View 与后台 I/O 调度验收

### 新源码 SPDX 头验收

### GPL 源码和许可证交付验收

### 新依赖 AboutLibraries 输出验收

## 第九轮生命周期、无障碍和实施门禁测试







### 图标按钮 content description 验收

### 48dp 最小触控目标验收










### 简体中文界面完整性验收




### Activity 重建后 SAF 结果关联验收

### 重复 SAF 结果不重复导入验收

### 多窗口第二会话只读验收

### 保存前外部文件修改检测验收

### 外部修改重新加载或取消保存验收



### 草稿损坏隔离和已保存版本回退验收

### 撤销栈迁移失败仅丢弃历史验收

### 崩溃后临时文件事务识别验收



### M0 构建基线门禁记录验收

### M1 只读导入门禁验收

### M2 往返核心门禁验收

### M3 rows 编辑门禁验收

### M4 全布局与样式门禁验收

### M5 高级代码与恢复门禁验收

### M6 导出安装门禁验收

### M7 Release 门禁验收

### 人工测试稳定 test ID 验收

### 人工测试 API 和设备条件记录验收

### 人工测试通过失败阻塞不适用状态验收

## 异常恢复

### Lua 语法错误界面处理验收

### 样式文件加载失败回退验收

### 键盘文件加载失败处理验收

### 图片资源缺失处理验收

### 字体资源缺失处理验收

### 音效资源缺失处理验收

### 应用旋转后编辑状态恢复验收

### 应用切后台后草稿恢复验收

### 应用进程被系统回收后项目恢复验收

### 导出失败后原项目不受影响验收

### 预览异常后的降级界面验收
